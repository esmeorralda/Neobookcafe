<div class="bg-white border border-gray-200 rounded-lg pt-4 pb-6 pl-6 pr-6 shadow-sm w-full">
  <div class="flex items-center py-2 mb-2 space-x-4">  <!-- 간격 1rem -->
    <span class="font-semibold text-base"><%= title %></span>
   <% if subtitle.present? %>
  <span class="text-xs text-blue-500 whitespace-nowrap"><%= subtitle %> &rarr;</span>
<% end %>

  </div>

  <hr class="border-t border-[#bfae9b] w-[20%] mx-auto my-4 opacity-60">

  <ul class="space-y-3 text-sm text-gray-700">
    <% items.each do |item| %>
      <li class="flex justify-between items-center py-1 px-2">
        <span><%= item %></span>
        <div class="flex gap-4 text-gray-400 text-xs">
          ❤️ 77 💬 77
        </div>
      </li>
    <% end %>
  </ul>
</div>
